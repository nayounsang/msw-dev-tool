{
  "name": "msw-dev-tool-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "resolutions": {
    "msw": "2.7.3"
  },
  "scripts": {
    "build": "yarn build:packages && yarn build:docs",
    "build:packages": "turbo run build --filter=@msw-dev-tool/core --filter=@msw-dev-tool/cli-core --filter=@msw-dev-tool/react --filter=@msw-dev-tool/node-cli --filter=@msw-dev-tool/browser-cli --filter=msw-dev-tool",
    "build:msw-dev-tool": "yarn workspace msw-dev-tool build",
    "build:core": "yarn workspace @msw-dev-tool/core build",
    "build:react": "yarn workspace @msw-dev-tool/react build",
    "build:node-cli": "yarn workspace @msw-dev-tool/node-cli build",
    "build:docs": "yarn workspace docs build",
    "dev": "turbo run dev --filter=docs... --filter=example...",
    "dev:example": "turbo run dev --filter=example...",
    "typecheck": "yarn workspace @msw-dev-tool/core typecheck && yarn workspace @msw-dev-tool/cli-core typecheck && yarn workspace @msw-dev-tool/react typecheck && yarn workspace @msw-dev-tool/node-cli typecheck && yarn workspace @msw-dev-tool/browser-cli typecheck && yarn workspace msw-dev-tool typecheck",
    "lint": "yarn workspace @msw-dev-tool/core lint && yarn workspace @msw-dev-tool/react lint",
    "test": "yarn workspace @msw-dev-tool/core test && yarn workspace @msw-dev-tool/cli-core test && yarn workspace @msw-dev-tool/node-cli test && yarn workspace @msw-dev-tool/browser-cli test",
    "test:coverage": "yarn workspace @msw-dev-tool/core exec vitest run --coverage && yarn workspace @msw-dev-tool/cli-core exec vitest run --coverage && yarn workspace @msw-dev-tool/node-cli exec vitest run --coverage && yarn workspace @msw-dev-tool/browser-cli exec vitest run --coverage",
    "version-packages": "yarn changeset version && yarn install --mode=update-lockfile",
    "changeset": "changeset",
    "clean": "yarn workspace msw-dev-tool clean && yarn workspace docs clean && rm -rf node_modules",
    "resolve-workspace-protocol": "ts-node scripts/resolve-workspace-protocol.ts",
    "release": "yarn resolve-workspace-protocol && yarn build:packages && yarn changeset publish"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.10",
    "ts-node": "^10.9.2",
    "turbo": "^2.5.4",
    "typescript": "^5.8.3",
    "vitest": "^4.1.10"
  },
  "packageManager": "yarn@4.5.3",
  "dependencies": {
    "@changesets/cli": "^2.28.1"
  }
}
