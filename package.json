{
  "name": "msw-dev-tool-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspace msw-dev-tool build",
    "version-packages": "yarn changeset version",
    "changeset": "changeset"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "packageManager": "yarn@4.5.3",
  "dependencies": {
    "@changesets/cli": "^2.28.1"
  }
}
