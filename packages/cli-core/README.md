# @msw-dev-tool/cli-core

Internal shared command implementation for the published `@msw-dev-tool/node-cli`
and `@msw-dev-tool/browser-cli` packages. It is versioned and published so those
CLI packages can resolve their runtime dependency when installed from npm.
