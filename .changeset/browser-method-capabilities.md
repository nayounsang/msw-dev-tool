---
"@msw-dev-tool/core": minor
"@msw-dev-tool/browser-cli": patch
---

Replace the global browser control protocol gate with method-level capability checks while preserving compatibility metadata for older CLI versions.
