---
"@msw-dev-tool/react": minor
---

Export `HandlerDebugger`, `HandlerTable`, and `AddTempHandlerForm` for custom composition. Replace Radix UI/Tailwind with Base UI and plain CSS, removing ThemeProvider and PortalContainerProvider.
