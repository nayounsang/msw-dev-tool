---
"@msw-dev-tool/core": minor
"@msw-dev-tool/react": minor
"@msw-dev-tool/cli-core": minor
"@msw-dev-tool/browser-cli": minor
"@msw-dev-tool/node-cli": minor
---

Unify temporary and custom response configuration across HTTP and WebSocket APIs, CLIs, persistence, and React controls. HTTP custom responses now support the full temporary response settings, while WebSocket default and custom responses each own their delay and repeat schedule.
